/**
 * Provider display names for AI providers.
 * Extracted to a separate file to satisfy react-refresh constraints
 * (component files may only export components).
 */
export const AI_PROVIDER_NAMES: Record<string, string> = {
  claude: 'Claude',
  openai: 'OpenAI',
  gemini: 'Gemini',
  perplexity: 'Perplexity',
  openrouter: 'OpenRouter',
};
