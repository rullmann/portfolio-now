{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables the default permissions with security restrictions",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-message",
    "dialog:allow-ask",
    "shell:allow-open",
    "store:default"
  ],
  "context": {
    "security_note": "SECURITY: fs and shell permissions removed. File operations are handled through Tauri commands with path validation. Shell execution is disabled except for shell:allow-open (opening URLs/files with default app). store:default is for secure API key storage."
  }
}
